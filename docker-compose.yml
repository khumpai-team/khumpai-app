services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: khumpai
    ports:
      - "5432:5432"
    volumes:
      - khumpai_pgdata:/var/lib/postgresql/data
volumes:
  khumpai_pgdata:
